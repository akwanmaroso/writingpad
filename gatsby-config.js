module.exports = {
    plugins: [
        {
            resolve: `gatsby-plugin-netlify-identity`,
            options: {
                url: `https://writingpad.netlify.com`
            }
        }
    ]
}